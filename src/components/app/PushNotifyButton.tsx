/**
 * Push Notification Button — DISABLED
 *
 * Notifications disabled to save bandwidth.
 * To re-enable, restore the notif backend functions first.
 */
export function PushNotifyButton() {
  return null;
}
